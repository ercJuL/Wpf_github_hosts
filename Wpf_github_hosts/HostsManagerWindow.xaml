<mah:MetroWindow x:Class="Wpf_github_hosts.HostsManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Wpf_github_hosts"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.FontSize="14"
        FontFamily="SimHei"
        xmlns:System="clr-namespace:System;assembly=mscorlib"
        mc:Ignorable="d"
        Title="HostsManagerWindow" Height="450" Width="400">
    <Grid>
        <DataGrid>
            <DataGrid.Columns>
                <DataGridTextColumn Header="IP" Binding="{Binding Ip}"/>
                <DataGridTextColumn Header="Domain" Binding="{Binding Domain}"/>
                <DataGridTemplateColumn Header="DataGridTemplateColumn"/>
                <DataGridCheckBoxColumn Header="DataGridCheckBoxColumn"/>
                <DataGridComboBoxColumn Header="DataGridComboBoxColumn"/>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</mah:MetroWindow>
